# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
.env.*.local

# Editor and tooling
.claude/
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Which one-time trial codes have been spent. Runtime state, not config —
# the issued list itself lives in ACCESS_CODES_TRIAL.
trial-codes-used.json
trial-codes-used.json.tmp

# Which one-time trial codes have been issued to which email addresses. Runtime 
# state, not config — the issued list itself lives in TRIAL_CODES_ISSUED_STATE.
trial-codes-issued.json
trial-codes-issued.json.tmp

# Logs
*.log
npm-debug.log*
